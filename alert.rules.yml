groups:
  - name: sentiment-drift-rules
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.693
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: Sentiment model confidence drift detected
          description: prediction_confidence_score has stayed below 0.693 for more than 1 minute.
